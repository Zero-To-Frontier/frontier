# 참여 방법

## 새 회차 만들기

기록 담당자가 `templates/session/`을 저장소 루트의 실제 모임 날짜(`YYYYMMDD/`)로 복사합니다. `개인명/` 폴더를 참여자 이름으로 변경하고, 다른 참여자의 폴더도 같은 템플릿으로 만듭니다. 회차 README에 탐구 단계·도구의 정확한 링크·탐구 목적·일정·담당자를 적습니다. 초반에는 공통 도구를, 이후에는 개인별 선택 도구와 선정 이유를 기록합니다. 개인별 작업과 질문은 각자가 정하고 변경할 수 있습니다.

주제와 날짜는 모임에서 합의한 값을 사용합니다. 기존 날짜 폴더를 덮어쓰지 않습니다.

## 개인 작업

처음에는 공용 `main` 브랜치에 직접 커밋하고 push합니다. 아래 예시의 날짜와 이름은 실제 경로로 바꾸세요. 저장소 폴더 안에서 실행합니다.

```bash
# 작업 시작 전: 이전 작업을 커밋한 상태에서 실행
 git pull --rebase

# 자기 폴더에서 기록·실험 후 해당 경로만 추가
 git add YYYYMMDD/개인명/
 git diff --cached
 git commit -m "docs: 회차 개인 탐구 기록"

# 다른 사람의 변경을 반영하고 업로드
 git pull --rebase
 git push
```

- 자기 폴더를 중심으로 수정합니다. 회의록과 회차 안내는 담당자에게 반영을 요청합니다.
- 다른 사람이 먼저 push했다면 파일이 달라도 push가 거절될 수 있습니다. `git pull --rebase` 후 다시 push합니다.
- 실제 충돌이 나면 해당 작성자와 내용을 확인하고 충돌 표시를 정리한 뒤, 수정 파일을 `git add`하고 `git rebase --continue`합니다. 해결 방향이 불명확하면 `git rebase --abort`로 이번 rebase를 취소하고 함께 확인합니다.
- 공용 브랜치에는 강제 push(`--force`, `--force-with-lease`)를 하지 않습니다.
- 다른 사람의 폴더나 공통 운영 원칙을 바꿀 때는 먼저 상의합니다. 검토가 필요한 변경은 선택적으로 PR을 사용합니다.
- 인증 정보와 실제 비밀값은 커밋하지 않습니다. 실험에 필요한 설정 예시는 `.env.example`처럼 값이 없는 예제로 남깁니다.

## 모임 전후

1. 모임 전: 초반에는 공통 도구를, 이후에는 각자 선택한 도구를 사용하고, 개인 폴더에 진행 내용·증거·자신의 해석·남은 의문을 먼저 남깁니다.
2. 모임 중: 기록 담당자가 `meeting.md`에 각자의 관찰과 근거, 관점의 차이를 기록합니다. 서로 다른 도구를 다루는 단계에서는 프로젝트의 배경과 생태계 흐름에 대한 연결점도 나눕니다. 의견을 하나로 통일할 필요는 없습니다.
3. 모임 후: 담당자가 회의록을 커밋하고 push합니다. 초반에는 함께, 이후에는 각자 탐구 연장 여부와 다음 도구를 정해 공유하고, 일정도 합의한 만큼만 기록합니다.
4. Frontier 도입 기간 후: 준비 부담, 토론에서 얻은 것, 다음 회차에 줄이거나 바꿀 것을 돌아봅니다.
